apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello-app
  template:
    metadata:
      labels:
        app: hello-app
    spec:
      containers:
      - name: hello-app
        # IMPORTANTE: Use seu usuário do Docker Hub aqui.
        # Esta imagem será atualizada pelo GitHub Actions depois.
        image: laissansara/hello-app:b40404edda14e5a72545d516f3ddf62bb3f4b3de
        ports:
        - containerPort: 8000